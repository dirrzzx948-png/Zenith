{
  "name": "zenith",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p www && cp -r public/* www/",
    "cap": "cap"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.0",
    "@capacitor/android": "^7.4.0"
  }
}
